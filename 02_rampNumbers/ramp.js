var stringCount = "1234";

}
function findRamp (stringToBeCounted) {
  var i;
  var count = 0;
  for (i = 0; i < stringToBeCounted.length; i += 1) {

    if (stringToBeCounted[i] <  ) {
      Count += stringToBeCounted[i];
    }
    return count;
  }
}
var count = findRamp (stringCount);
console.log(count);
